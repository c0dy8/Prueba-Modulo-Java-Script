{
  "name": "crudtask",
  "version": "1.0.0",
  "description": "Gestión de tareas académicas - API falsa",
  "scripts": {
    "server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
